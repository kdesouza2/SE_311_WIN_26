**Architecture Style**
The first, and most important decision of a software project

**Distributed Programming**
Distrubuted v. Concurrent
    Architectures: Socket, RMI, Http

**Client/Server Style**
Client/Server systems are the most common specialization 

**Socket**
A socket is a reliable connection for the transmission of data between two hosts
Operations:
1. Connect to a remote machine
2.
3.
4.

**Server**

**TCP/IP Protocol Stacks**
The lowest layer handles communication between 2 computers
The internet layer handles routing of packets across the network
The transport layer guarantees that packets are error-free and that they are received in the same order as they are sent
The application layer supports application-specific protocols 
    Eg. HTTP, SMTP, FTP, ...


